# Copy to .env and set a password. The real .env is gitignored.
# MSSQL requires at least 8 characters with upper, lower, digit and symbol.
DB_SERVER=localhost
DB_DATABASE=dw_hgg_database
DB_USER=sa
DB_PASSWORD=ChangeMe_123
